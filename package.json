{
  "name": "mern-exchange",
  "version": "1.2.0",
  "description": "exchange app on react & express",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "npm run start --prefix client",
    "server": "nodemon index.js --prefix server",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "serverinst":"npm install --prefix server",
    "clientinst":"npm install --prefix client",
    "inst": "concurrently \"npm run serverinst\" \"npm run clientinst\""
  },
  "keywords": [
    "mern",
    "react",
    "exchange"
  ],
  "author": "deus_kh",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.3"
  }
}
